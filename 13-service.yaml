kind: Service
apiVersion: v1
metadata:
  name: nginx
spec:
  selector:
    name: forntend
    project: expense
    component: forntend
    environment: dev
  ports:
  - protocol: TCP
    port: 80 #service port
    targetPort: 80  #target port